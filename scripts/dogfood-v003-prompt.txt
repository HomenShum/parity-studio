Premium agentic-AI workspace called "Parity Studio v0.0.3". Single-page web app, dark theme, ultra-high-performance feel. The product takes a sketch or text prompt, runs it through gpt-image-2 → componentized ui_kit → 12-check boolean parity rubric → iterate, and exports a coding-agent-ready bundle for Claude Code / Cursor / Windsurf.

LAYOUT — 4 zones, top to bottom + left-to-right inside the main row:

ZONE 1 — TOP BAR (h=48px, hairline bottom border):
- Left: 28x28 rounded terracotta logo tile with bold serif "P", then "Parity Studio" wordmark in semi-bold Manrope, then "v0.0.3" pill in JetBrains Mono 10px
- Right: green status dot + "Convex blissful-pig-998" mono 11px, separator, "GitHub" link

ZONE 2 — INPUT BAR (h=72px, full width):
- Left: "+ source image" attachment button with subtle border
- Center: large rounded input "describe the UI, or just upload a sketch..." with terracotta focus ring
- Right: solid terracotta "Generate" button
- Below the row, two-column meta line: left has a checkbox "mockup-first (gpt-image-2)" with terracotta accent, right says "est. cost $0.10-0.80 · +$0.16 if mockup-first"

ZONE 3 — MAIN ROW (3 columns):

3a. FILES PANEL (left rail w=320px, dark surface):
- "FILES" section label (mono 11px uppercase tracking-wide muted)
- Live tree showing ui_kits/<slug>/ with index.html, components/*.tsx, tokens.css, manifest.json, README.md as files stream in
- "HANDOFF" section label below
- Full-width "Export ZIP" button with terracotta-tinted hover

3b. PREVIEW PANE (center, fills remaining width, slightly darker surface):
- Header strip h=40px: left "ARTIFACT PREVIEW v1" + tiny terracotta "comment mode · drag to pin" badge when comment mode is active, right viewport tabs "desktop / tablet" mono 10px
- Below: large white iframe-style canvas filling all remaining space showing a real generated UI
- Comment overlay: dashed terracotta rectangles on top of pinned regions, popover with textarea + "save" / "cancel" when drawing a new bbox

3c. ACTION SIDEBAR (right rail w=288px, dark surface):
- "PIPELINE" section label
- 5 stage rows with colored dots: image-gen (gpt-image-2), generate, decompose, verify·deterministic, verify·visual (unavailable in-app), iterate. Active stage row has pulsing yellow dot. Each row shows latency + model badge in JetBrains Mono on the right

- "PARITY" section label
- Bordered card with large mono "11 /. 12 CHECKS" + bounded-enum status pill underneath ("VERIFIED" / "NEEDS_REVIEW" / "NEEDS_ITERATION" / "FAILED" / "UNAVAILABLE" — colored: green / amber / amber / red / muted)

- "COST · TELEMETRY" section label — THIS IS THE PREMIUM HERO
- Big total at the top: terracotta JetBrains Mono 22px "$0.4123" with small "TOTAL" mono uppercase 10px label above it
- Below: per-stage breakdown rows separated by hairline borders. Each row:
  - Stage label: "image-gen" / "generate" / "decompose" / "verify · deterministic" / "iterate-1"
  - Model id in JetBrains Mono 10px muted: "gpt-image-2" / "moonshotai/kimi-k2.6" / "google/gemini-3.1-pro-preview"
  - Token count + latency mono 10px faint: "1,234 in · 5,678 out · 12.3s"
  - Cost on the right: JetBrains Mono 12px "$0.1600"

- "TOOLS" section label
- 3 outlined full-width buttons stacked: "Comment mode" (toggleable, terracotta when active), "Iterate now" (shows "Iterate now (3 comments)" badge when there are open comments), "Hand off to Claude Code"

VISUAL LANGUAGE:
- Background canvas: deep warm dark, near-black with slight warm tint #1a1b26
- Surfaces: half-shade lighter #24253a (panels) and #2d2e42 (preview pane bg)
- Borders: hairline rgba white at 6%, 12% on emphasis
- Primary accent: warm terracotta #c96442 used ONLY for: logo tile, focus ring, Generate button, active toggle, total-cost number. Never decorative.
- Status colors: success #22c55e (small dot only), warn #f3c969, danger #ff7b72
- Body text #e4e4e7 with #9ca3af muted and #6b7280 faint
- Typography: Manrope for body, JetBrains Mono for labels/numbers/code/model-ids
- Section labels: 11px uppercase letter-spacing 0.18em, JetBrains Mono, muted gray
- Generous whitespace between zones
- No gradients on text. No shadows on cards. No glow effects. Restrained.
- Subtle backdrop blur on glass panels but nothing flashy

OVERALL VIBE: Linear meets a high-end agentic chat UI. Premium developer/designer tool. Every element earns its place. The cost telemetry is the hero — this is the only AI design tool that shows you exactly what you're paying for, per stage, per model, per token, in real time.
